Run: 

To use the NPM package functions ensure you have run "npm install -g rimraf" in your terminal. This will allow you to run "testbuild:prod" which will delete your node modules and re-install them and then finally host the deployed code for testing locally.